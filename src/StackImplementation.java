public class StackImplementation {

    //the concepts of prefix and postfix
    //if a opening bracket is faced it will staff it inside the stack
    //if an operator is faced if the stack is empty it will be stuffed inside the stack
    //if there is already some operator in the stack , the most top operator of the stack will
    //compared with the current operator the and the stack must maintain the order of presidence
    //among the operators

    //if an operand is faced that will be added to the desired string.


    public static void main(String[] args) {

        // in this program we have to take an array



    }
}
